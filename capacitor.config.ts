import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.qualiwatt.huaweisalesmanager',
  appName: 'Huawei Sales Manager',
  webDir: 'dist',
  ios: {
    contentInset: 'always',
  },
};

export default config;
